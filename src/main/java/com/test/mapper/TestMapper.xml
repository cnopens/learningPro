<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.test.mapper.TestMapper">
    <resultMap id="BaseResultMap" type="com.test.model.Test">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Nov 12 23:51:09 CST 2013.
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP"/>
        <result column="update_date" property="updateDate" jdbcType="TIMESTAMP"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Nov 12 23:51:09 CST 2013.
        -->
        id, name, type, create_date, update_date
    </sql>
    <select id="getTests" resultMap="BaseResultMap" parameterType="map">
        SELECT id, name, type, create_date, update_date
        FROM test
        WHERE 1=1
        <if test="type!=null and type!=''">
            AND type = #{type}
        </if>
        limit #{startIndex},#{pageSize}
    </select>

    <select id="getTestsCount" resultType="java.lang.Integer" parameterType="map">
        SELECT COUNT(1)
        FROM test
        WHERE 1=1
        <if test="type!=null and type!=''">
            AND type = #{type}
        </if>
    </select>

    <insert id="addTest" parameterType="Test">
        insert into test (name, type, create_date, update_date) values (#{name}, #{type}, #{createDate}, #{updateDate})
    </insert>
    <delete id="delTest" parameterType="Integer">
        delete from test
        where id = #{id}
    </delete>
    
    <select id="getTestById" parameterType="Integer" resultMap="Test">
        select * from test where id=#{id}
    </select>
    <update id="editTest" parameterType="Test">
        update test set name=#{name} ,set type=#{type} ,set create_date =#{createDate} ,set update_date =#{updateDate} where id = #{id}
    </update>
</mapper>